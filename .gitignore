# Large sample media — not versioned
*.mp4
*.mov
*.m4v

# Backups created by the app
.backups/

# Brainstorm companion artifacts
.superpowers/

# macOS
.DS_Store

# Xcode / Swift build artifacts
.build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
.swiftpm/
Package.resolved

# Your own working video/subtitle files kept out of the repo (open the app on
# them locally). A small neutral fixture lives in Tests/.../Resources/sample.srt.
/*.srt

# Built app bundle (regenerate via scripts/build-app.sh)
*.app/
